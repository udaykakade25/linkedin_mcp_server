# LinkedIn API Configuration (Required)
# Generate this token through LinkedIn's OAuth2 flow or developer console
# Required scopes: r_liteprofile (or r_basicprofile), w_member_social
LINKEDIN_ACCESS_TOKEN=YOUR_LINKEDIN_ACCESS_TOKEN_HERE

# Server Configuration (Optional)
# Port for the MCP server to listen on
LINKEDIN_MCP_SERVER_PORT=5000

# Optional: LinkedIn App Credentials (for reference or future OAuth implementation)
# These are not used in the current MCP server implementation
# LINKEDIN_CLIENT_ID=YOUR_LINKEDIN_CLIENT_ID_HERE
# LINKEDIN_CLIENT_SECRET=YOUR_LINKEDIN_CLIENT_SECRET_HERE

# Instructions for obtaining LinkedIn Access Token:
# 1. Go to https://www.linkedin.com/developers/
# 2. Create a new app or use an existing one
# 3. Configure the app with required scopes:
#    - r_liteprofile (for basic profile access)
#    - w_member_social (for posting content)
# 4. Use LinkedIn's OAuth2 flow to get an access token
# 5. Set the token in LINKEDIN_ACCESS_TOKEN above
